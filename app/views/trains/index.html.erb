<h1>Train Time</h1>

<div class="row">
  <div class="col-xs-12 col-sm-4">

    <%= text_field_tag :station, nil, class: "form-control", autocomplete: "off" %>
    <ul id="results"></ul>

  </div>
</div>

<!-- 
<table class="table table-striped">
  <% @trains.each do |train| %>
    <% train.stations.each do |station| %>

    <tr>
      <td><%= station.name %></td>
      <td><%= train.name %></td>
    </tr>

    <% end %>
  <% end %>
</table> -->
